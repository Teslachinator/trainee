// import React, { Children, useState } from "react";

// const NumBtn = ({ Children }) => {
//   const [number, setNumber] = useState(4);
//   console.log({ Children });

//   return <button>{() => setNumber(Children)}</button>;
// };

// export default NumBtn;
